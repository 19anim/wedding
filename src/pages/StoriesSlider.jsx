import StorySlider from "../components/newStorySlider/StorySlider";

const StoriesSlider = ({ currentDeviceHeight }) => {
  return <StorySlider currentDeviceHeight={currentDeviceHeight} />;
};

export default StoriesSlider;
